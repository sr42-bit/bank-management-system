spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: bankuser
    password: bankpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration