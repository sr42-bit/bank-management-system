spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/bank_clean_architecture?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: bankuser
    password: bankpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration